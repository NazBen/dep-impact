language: python

python:
  - "2.7"
  - "3.5"

os: linux

sudo: false

addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - gcc
    - gfortran
    - libgsl0-dev

notifications:
  email: false
   
install: source build_tools/install.sh

script: nosetests -v
