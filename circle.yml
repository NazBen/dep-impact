dependencies:
  #XXX: use the cache better
  pre:
    - sudo apt-get update
    - sudo apt-get install gcc gfortran libgsl0-dev
  override:
    - source ./build_tools/install.sh:
        timeout: 1024

test:
  override: 
    - nosetests:
        timeout: 3600